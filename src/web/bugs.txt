When targeting a multi-byte address-definition we need to show offset from name. For instance:
|| 88:89 || scrCursor || Screen cursor ||

If we do this:

STA >$89

We don't want
STA >scrCursor

Maybe

STA >scrCursor+1

-----------------------------------------------