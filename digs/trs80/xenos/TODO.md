The long text strings need to broken up over multiple lines.

Create the merge tool to put the update data back in the files.

Destination rooms can have descriptions too. Like "PLAYER" for room 1.

Fix the "multi switch" decode to handle all functions

Finish the unknown regular commands

Finish all the subroutines

Use the linux z80 python emulator to play. Can we use this emulator on the PC?

DONE Add the phrases to the phrase numbers.